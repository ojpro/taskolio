import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <div className="mx-auto mt-8 shadow bg-white text-center p-4 max-w-fit">
        <h1 className="text-2xl font-bold text-blue-400"> ☑ Taskolio </h1>
    </div>
  </React.StrictMode>,
)
